# 需求点 URL 清单

更新日期：2026-03-24  
项目：`monitor-console`  
前端宿主：`web-audio`  
后端宿主：`egg-audio`

## 测试基线

- 页面与功能测试 skill：`playwright`
- 实际浏览器：`Google Chrome`
- 浏览器可执行文件：`/opt/google/chrome/chrome`
- 浏览器脚本：`web-audio/tests/e2e/chrome-pages-smoke.mjs`
- 最新报告：`web-audio/tests/e2e/artifacts/chrome-pages-smoke-report.json`

## 清单说明

- “已测试”表示已有实际验证证据。
- “页面测试”指真实浏览器访问与交互验证。
- “后端测试”指后端 service test 或接口验活证据。
- “配置型 URL”表示不是固定产品路由，而是由环境变量组合出的真实探测地址。

## 需求点清单

| 需求点 | URL 清单 | 当前状态 | 是否测试 | 测试方式 / 证据 |
|---|---|---|---|---|
| 正式登录入口 | `/user/login` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/user/login` 为 `passed: true` |
| 真实登录提交 | `POST /api/auth/login` | 已实现 | 已测试 | 真实 Chrome 登录流程命中 `/api/auth/login`；报告 `login.matchedApis` 有记录 |
| 登录后进入正式工作台 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 登录后最终 URL 为 `/monitor`；报告 `login.finalUrl` 为 `/monitor` |
| 工作台主页面 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/monitor` 为 `passed: true` |
| 主机监控列表页 | `/hosts` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/hosts` 为 `passed: true` |
| 主机监控详情页 | `/hosts/:id`（当前验收 ` /hosts/egg-api `） | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/hosts/egg-api` 为 `passed: true` |
| 服务监控列表页 | `/services` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/services` 为 `passed: true` |
| 服务监控详情页 | `/services/:id`（当前验收 ` /services/audio `） | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/services/audio` 为 `passed: true` |
| 数据库监控页 | `/databases` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/databases` 为 `passed: true` |
| 存储 / 文件服务监控页 | `/storage` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/storage` 为 `passed: true` |
| 网络拓扑页 | `/topology` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/topology` 为 `passed: true` |
| 告警中心页 | `/alerts` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/alerts` 为 `passed: true` |
| 告警历史页 | `/alerts/history` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/alerts/history` 为 `passed: true` |
| 事件中心页 | `/events` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/events` 为 `passed: true` |
| 实时日志页 | `/logs/live` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/logs/live` 为 `passed: true` |
| 日志检索页 | `/logs/search` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/logs/search` 为 `passed: true` |
| 诊断建议页 | `/diagnostics` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/diagnostics` 为 `passed: true` |
| 指标趋势页 | `/metrics` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/metrics` 为 `passed: true` |
| 可用率报表页 | `/reports/availability` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/reports/availability` 为 `passed: true` |
| 容量分析页 | `/reports/capacity` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/reports/capacity` 为 `passed: true` |
| 监控策略配置页 | `/settings/monitor` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/settings/monitor` 为 `passed: true` |
| 通知配置页 | `/settings/notifications` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/settings/notifications` 为 `passed: true` |
| 数据源配置页 | `/settings/datasources` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/settings/datasources` 为 `passed: true` |
| 用户与权限页 | `/settings/users` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/settings/users` 为 `passed: true` |
| 根入口统一收口 | `/` → `/monitor` | 已实现 | 已测试 | 真实 Chrome 页面测试通过；报告中 `routes[].path=/` 为 `passed: true`，`finalUrl` 为 `/monitor` |
| 旧业务页彻底移除 | `/welcome` | 已删除，当前 404 | 已测试 | 真实 Chrome 页面测试通过；报告中 `welcome-removed` 为 `passed: true` |
| 旧业务页彻底移除 | `/group` | 已删除，当前 404 | 已测试 | 真实 Chrome 页面测试通过；报告中 `group-removed` 为 `passed: true` |
| 旧业务页彻底移除 | `/list` | 已删除，当前 404 | 已测试 | 真实 Chrome 页面测试通过；报告中 `list-removed` 为 `passed: true` |
| 旧业务页彻底移除 | `/audio` | 已删除，当前 404 | 已测试 | 真实 Chrome 页面测试通过；报告中 `audio-removed` 为 `passed: true` |
| 旧业务页彻底移除 | `/file` | 已删除，当前 404 | 已测试 | 真实 Chrome 页面测试通过；报告中 `file-removed` 为 `passed: true` |
| 未知路径收口 | `*`（例如 `/__monitor_unknown_route__`） | 已实现，当前 404 | 已测试 | 真实 Chrome 页面测试通过；报告中 `not-found` 为 `passed: true` |
| 监控页真实主数据接口 | `GET /api/monitor/dashboard` | 已实现 | 已测试 | 真实 Chrome 访问 `/monitor` 时命中；报告中 `routes[].matchedApis` 含 `/api/monitor/dashboard` |
| 监控页验活接口 | `GET /api/monitor/healthz` | 已实现 | 已测试 | 已有后端验活与项目验证记录；并在本地 E2E 启动前作为 backend health check 使用 |
| 当前用户信息恢复 | `GET /api/user/info` | 已实现 | 已测试 | 2026-03-24 实际调用通过，返回 `{"success": true, "username": "超级管理员", "role": 0}` |
| 退出登录 | `POST /api/auth/logout` | 已实现 | 已测试 | 2026-03-24 实际调用通过，返回 `{"success": true, "message": "登出成功"}` |
| 告警中心展示 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 功能测试通过；`monitor-alert-list` 可见 |
| 诊断建议展示 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 功能测试通过；`monitor-diagnostic-list` 可见 |
| 桌面通知入口 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 功能测试通过；通知按钮存在 |
| 监控视图切换 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 功能测试通过：列表视图、趋势视图、网络拓扑切换均通过 |
| 监控筛选切换 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 功能测试通过：仅显示数据库 / 全部显示 均通过 |
| 节点详情查看 | `/monitor` | 已实现 | 已测试 | 真实 Chrome 功能测试通过：点击 `MySQL` 节点后详情区标题更新 |
| 监控页轮询配置 | `GET /api/monitor/dashboard` 返回 `refresh` | 已实现 | 已测试 | 后端 service test 已断言 `refresh.intervalSeconds` 与 `refresh.staleAfterSeconds` |
| 监控阈值配置 | `GET /api/monitor/dashboard` 返回 `config.thresholds` | 已实现 | 已测试 | 后端 service test 已断言阈值字段返回 |
| 告警 / 诊断 capability 配置 | `GET /api/monitor/dashboard` 返回 `capabilities` | 已实现 | 已测试 | 后端 service test 已断言 `capabilities.diagnostics` / `capabilities.notifications` |
| 上游真实探测（配置型） | `BASE_URL + MONITOR_UPSTREAM_PROBE_PATH` | 已实现 | 已测试 | 后端 service test 已断言 `config.probes.upstreamPath` 与 probe 数据返回 |
| 文件服务真实探测（配置型） | `FILE_BASE_URL + MONITOR_FILE_PROBE_PATH` | 已实现 | 已测试 | 后端 service test 已断言 `config.probes.filePath` 与 probe 数据返回 |
| 健康接口返回 probe 明细 | `GET /api/monitor/healthz` 返回 `probes.upstream` / `probes.file` | 已实现 | 已测试 | 后端 service test 已断言 health payload probe details |
| 告警去重稳定性 | `/monitor` 桌面通知基于 `dedupeKey + lastChangeAt` | 已实现 | 已测试 | 后端 service test 已断言 unchanged polls 下告警时间戳稳定 |

## 当前正式 URL 总表

### 前端正式 URL

- `/user/login`
- `/monitor`
- `/hosts`
- `/hosts/:id`
- `/services`
- `/services/:id`
- `/databases`
- `/storage`
- `/topology`
- `/alerts`
- `/alerts/history`
- `/events`
- `/logs/live`
- `/logs/search`
- `/diagnostics`
- `/metrics`
- `/reports/availability`
- `/reports/capacity`
- `/settings/monitor`
- `/settings/notifications`
- `/settings/datasources`
- `/settings/users`
- `/` → `/monitor`
- `*` → `404`

### 已删除旧前端 URL

- `/welcome` → 当前 404
- `/group` → 当前 404
- `/list` → 当前 404
- `/audio` → 当前 404
- `/file` → 当前 404

### 当前正式后端 URL

- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/user/info`
- `GET /api/monitor/healthz`
- `GET /api/monitor/dashboard`

### 当前配置型真实探测 URL

- `BASE_URL + MONITOR_UPSTREAM_PROBE_PATH`
- `FILE_BASE_URL + MONITOR_FILE_PROBE_PATH`

## 本次已验证结果摘要

- `cd web-audio && npm run tsc` ✅
- `cd web-audio && npm run build` ✅
- `cd web-audio && npm run test:e2e:pages` ✅
  - `totalRoutes = 29`
  - `failedRoutes = []`
  - `totalFunctionalChecks = 8`
  - `failedFunctionalChecks = []`
- `agent-browser --session monitorqa ...` ✅
  - 通过 `agent-browser` skill 驱动浏览器逐页访问 21 个正式页面 URL
  - 报告文件：`web-audio/tests/e2e/artifacts/agent-browser-pages-report.json`
- `cd egg-audio && env DB_HOST=127.0.0.1 DB_PORT=3110 DB_USER=root DB_PASSWORD=dz123456 DB_NAME=dz_db_mysql node node_modules/egg-bin/dist/bin/cli.js test test/app/service/monitor.test.js` ✅
