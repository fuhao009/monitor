# 页面任务队列

更新日期：2026-03-24

## 串行执行规则

- 每个页面单独作为一个 task。
- 一次只推进 **一个 task**。
- 当前 task 完成并验证后，才进入下一个 task。
- 每个 task 的完成标准至少包括：
  - 路由接入完成
  - 页面结构与交互落地
  - 对应真实数据或真实接口落地
  - `lsp_diagnostics` 无错误
  - `npm run tsc` 通过（若影响前端）
  - `npm run build` 通过
  - 真实 Chrome 页面测试补齐并通过
  - `AGENTS.md`、记忆文件同步更新

## Task 队列

### Task 01 — `/monitor` 首页总览仪表盘增强
- 类型：enhancement
- 状态：completed
- 目标：把当前单页监控板升级为真正的首页总览仪表盘
- 关键交付：平台摘要卡、快捷跳转、趋势摘要、活动告警摘要、最近事件区

### Task 02 — `/hosts` 主机监控列表页
- 类型：new page
- 状态：completed
- 目标：主机列表、状态筛选、搜索、资源摘要

### Task 03 — `/hosts/:id` 主机监控详情页
- 类型：new page
- 状态：completed
- 目标：单主机详情、资源趋势、最近事件与告警

### Task 04 — `/services` 服务监控列表页
- 类型：new page
- 状态：completed
- 目标：服务清单、健康状态、延迟、错误率、依赖入口

### Task 05 — `/services/:id` 服务监控详情页
- 类型：new page
- 状态：completed
- 目标：单服务详情、探针结果、趋势、依赖拓扑

### Task 06 — `/databases` 数据库监控页
- 类型：new page
- 状态：completed
- 目标：数据库连接、延迟、容量、慢查询摘要

### Task 07 — `/storage` 存储 / 文件服务监控页
- 类型：new page
- 状态：completed
- 目标：文件服务探针、容量、失败记录、链路状态

### Task 08 — `/alerts` 告警中心页
- 类型：new page
- 状态：completed
- 目标：活动告警列表、筛选、确认、静默、关联跳转

### Task 09 — `/events` 事件中心页
- 类型：new page
- 状态：completed
- 目标：事件流、状态变更、关键操作记录

### Task 10 — `/metrics` 指标趋势页
- 类型：new page
- 状态：completed
- 目标：关键指标趋势图、多时间范围切换、多指标对比

### Task 11 — `/settings/monitor` 监控策略配置页
- 类型：new page
- 状态：completed
- 目标：轮询、阈值、探针路径、能力开关配置

### Task 12 — `/topology` 网络拓扑页
- 类型：new page
- 状态：completed
- 目标：独立拓扑图、链路高亮、节点筛选、节点联动

### Task 13 — `/alerts/history` 告警历史页
- 类型：new page
- 状态：completed
- 目标：历史告警、恢复记录、趋势统计

### Task 14 — `/logs/live` 实时日志页
- 类型：new page
- 状态：completed
- 目标：实时日志流、暂停/恢复、筛选与检索

### Task 15 — `/logs/search` 日志检索页
- 类型：new page
- 状态：completed
- 目标：按时间范围和条件检索历史日志

### Task 16 — `/diagnostics` 诊断建议页
- 类型：new page
- 状态：completed
- 目标：系统风险列表、建议动作、关联资源跳转

### Task 17 — `/reports/availability` 可用率报表页
- 类型：new page
- 状态：completed
- 目标：SLA、可用率、故障时长报表

### Task 18 — `/reports/capacity` 容量分析页
- 类型：new page
- 状态：completed
- 目标：容量趋势、容量预警、容量预测

### Task 19 — `/settings/notifications` 通知配置页
- 类型：new page
- 状态：completed
- 目标：浏览器通知、邮件、Webhook 等通知规则配置

### Task 20 — `/settings/datasources` 数据源配置页
- 类型：new page
- 状态：completed
- 目标：Upstream / File / DB 数据源与探针配置

### Task 21 — `/settings/users` 用户与权限页
- 类型：new page
- 状态：completed
- 目标：用户、角色、权限范围与审计信息

### Task 22 — `/user/login` 登录页生产化增强
- 类型：enhancement
- 状态：pending
- 目标：品牌化、环境标识、审计提示、生产化体验完善

## 当前下一项

- 默认下一项：**Task 22 — `/user/login` 登录页生产化增强**
