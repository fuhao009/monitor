# 基础设施监控运维平台原型图清单

更新日期：2026-03-24

## 设计约束

- 基于当前正式宿主框架继续实现：`web-audio` + `egg-audio`
- 目标是生产化平台，不再继续单页大屏思路
- 数据要求真实、可配置、可验证
- 旧业务兼容页已删除；新页面按正式运维平台重新建设

## 页面原型清单

### 1. 登录页
- URL：`/user/login`
- 目标：账号密码登录、错误提示、环境标识、登录审计提示
- 当前状态：已存在基础版，后续做品牌化和生产化增强

### 2. 首页总览仪表盘
- URL：`/monitor`
- 目标：平台总览、核心资源摘要、告警摘要、趋势摘要、快捷跳转
- 当前状态：已实现总览增强版

### 3. 主机监控列表页
- URL：`/hosts`
- 目标：展示所有主机状态、CPU/内存/磁盘/网络占用、支持筛选和搜索
- 当前状态：已实现

### 4. 主机监控详情页
- URL：`/hosts/:id`
- 目标：单主机详情、资源趋势、网络情况、最近事件与告警
- 当前状态：已实现

### 5. 服务监控列表页
- URL：`/services`
- 目标：展示服务健康、延迟、错误率、依赖关系
- 当前状态：已实现

### 6. 服务监控详情页
- URL：`/services/:id`
- 目标：单服务探针结果、趋势、依赖拓扑、事件与告警
- 当前状态：已实现

### 7. 数据库监控页
- URL：`/databases`
- 目标：数据库连接、延迟、慢查询、容量与活跃连接情况
- 当前状态：已实现

### 8. 存储 / 文件服务监控页
- URL：`/storage`
- 目标：文件服务探针、容量摘要、失败记录、传输链路状态
- 当前状态：已实现

### 9. 网络拓扑页
- URL：`/topology`
- 目标：节点拓扑、链路状态、延迟、节点联动详情
- 当前状态：已实现独立页

### 10. 告警中心页
- URL：`/alerts`
- 目标：活动告警列表、筛选、确认、静默、关联跳转
- 当前状态：已实现独立页

### 11. 告警历史页
- URL：`/alerts/history`
- 目标：历史告警、恢复记录、趋势统计
- 当前状态：已实现基础版

### 12. 事件中心页
- URL：`/events`
- 目标：系统事件、状态变更、关键操作时间线
- 当前状态：已实现

### 13. 实时日志页
- URL：`/logs/live`
- 目标：实时日志流、级别筛选、来源筛选、关键词过滤
- 当前状态：已实现

### 14. 日志检索页
- URL：`/logs/search`
- 目标：按时间范围和条件检索历史日志
- 当前状态：已实现基础检索版

### 15. 诊断建议页
- URL：`/diagnostics`
- 目标：汇总系统风险、建议动作、关联资源
- 当前状态：已实现独立页

### 16. 指标趋势页
- URL：`/metrics`
- 目标：CPU/内存/延迟/带宽等趋势对比
- 当前状态：已实现趋势摘要页

### 17. 可用率报表页
- URL：`/reports/availability`
- 目标：服务可用率、SLA、故障时长统计
- 当前状态：已实现基础版

### 18. 容量分析页
- URL：`/reports/capacity`
- 目标：容量增长趋势、阈值预警、容量预测
- 当前状态：已实现基础版

### 19. 监控策略配置页
- URL：`/settings/monitor`
- 目标：轮询周期、阈值、探针路径、能力开关配置
- 当前状态：已实现配置展示页

### 20. 通知配置页
- URL：`/settings/notifications`
- 目标：浏览器通知、邮件、Webhook、短信等通知配置
- 当前状态：已实现配置展示页

### 21. 数据源配置页
- URL：`/settings/datasources`
- 目标：Upstream、File、DB 等数据源与探针配置
- 当前状态：已实现配置展示页

### 22. 用户与权限页
- URL：`/settings/users`
- 目标：运维台用户、角色、权限范围、审计信息
- 当前状态：已实现基础版

## 建议实施分期

### 第一阶段（平台骨架 + 监控主线）
- `/monitor`
- `/hosts`
- `/hosts/:id`
- `/services`
- `/services/:id`
- `/databases`
- `/storage`
- `/alerts`
- `/events`
- `/metrics`
- `/settings/monitor`

### 第二阶段（分析、历史、配置增强）
- `/topology`
- `/alerts/history`
- `/logs/live`
- `/logs/search`
- `/diagnostics`
- `/reports/availability`
- `/reports/capacity`
- `/settings/notifications`
- `/settings/datasources`
- `/settings/users`

## 当前默认执行顺序

后续按串行任务推进，默认从以下顺序开始：

1. `/monitor` 首页总览仪表盘增强
2. `/hosts`
3. `/hosts/:id`
4. `/services`
5. `/services/:id`
6. `/databases`
7. `/storage`
8. `/alerts`
9. `/events`
10. `/metrics`
11. `/settings/monitor`
